const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.nav-links');if(menuBtn&&nav){menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
const form=document.querySelector('#contactForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const n=document.querySelector('#name').value.trim();document.querySelector('#formMessage').textContent=`Thank you${n?`, ${n}`:''}! Your message is ready to send. Connect this form to Formspree, Netlify Forms, or your own email service when you publish the site.`;form.reset();})}
